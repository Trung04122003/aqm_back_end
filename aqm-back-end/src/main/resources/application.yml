# aqm-back-end/src/main/resources/application.yml (FIXED)
spring:
  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3307/air_quality_monitoring
    username: root
    password: "@Trung123"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  # ✅ REMOVE mvc.pathmatch - not needed for Spring Boot 3.x
  # mvc:
  #   pathmatch:
  #     matching-strategy: ant_path_matcher

  # ✅ Security config
  security:
    filter:
      order: 1

# JWT Configuration
jwt:
  secret: "a4b7c99e33f88b1466c2e9a9e3f04d3f!secure-key-2025"
  expiration: 86400000  # 1 day in milliseconds

# Springdoc OpenAPI (Swagger)
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    path: /v3/api-docs

# Logging
logging:
  level:
    org.springframework.security: DEBUG
    com.commander.aqm: DEBUG
    org.springframework.web: DEBUG

# Server
server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always