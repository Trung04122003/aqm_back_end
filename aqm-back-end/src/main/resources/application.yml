# src/main/resources/application.yml (FIXED)
spring:
  application:
    name: aqm-back-end

  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3307/air_quality_monitoring?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: "@Trung123"
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update  # ✅ CRITICAL: This will auto-create/update tables
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true

  # Email Configuration (Gmail SMTP)
  mail:
    host: smtp.gmail.com
    port: 587
    username: lethaitrung03@gmail.com  # Set in environment variable or .env
    password: sgde ysqh qteq tliz  # Use Gmail App Password, NOT your regular password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    default-encoding: UTF-8

  # Enable async for email sending
  task:
    execution:
      pool:
        core-size: 2
        max-size: 5
        queue-capacity: 100

# JWT Configuration
jwt:
  secret: "a4b7c99e33f88b1466c2e9a9e3f04d3f!secure-key-2025"
  expiration: 86400000 # 1 day

# Springdoc OpenAPI
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    path: /v3/api-docs

# Logging
logging:
  level:
    org.springframework.security: DEBUG
    com.commander.aqm: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# Server
server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always